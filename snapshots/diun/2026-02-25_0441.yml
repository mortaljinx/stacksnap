# Reconstructed by StackSnap from running containers.
# This is a best-effort snapshot — review before use.
services:
  diun:
    image: crazymax/diun:latest
    restart: unless-stopped
    volumes:
      - "/mnt/nvme/docker/diun:/data:rw"
      - "/var/run/docker.sock:/var/run/docker.sock:ro"
    environment:
      DIUN_NOTIF_WEBHOOK_HEADERS_CONTENT_TYPE: "application/json"
      TZ: "Europe/London"
      DIUN_PROVIDERS_DOCKER_WATCHBYDEFAULT: "true"
      DIUN_NOTIF_WEBHOOK_METHOD: "POST"
      DIUN_LOG_LEVEL: "info"
      DIUN_WATCH_SCHEDULE: "0 */6 * * *"
      DIUN_PROVIDERS_DOCKER: "true"
      DIUN_NOTIF_WEBHOOK_ENDPOINT: "http://smartarr-backend:8001/api/webhook/diun"
      PROFILER_PATH: "/profiler"
      DIUN_DB_PATH: "/data/diun.db"
