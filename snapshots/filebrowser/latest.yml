# Reconstructed by StackSnap from running containers.
# This is a best-effort snapshot — review before use.
services:
  filebrowser:
    image: filebrowser/filebrowser:latest
    restart: unless-stopped
    ports:
      - "3030:80"
      - ":::3030:80"
    volumes:
      - "/mnt/nvme/docker:/srv/docker:rw"
      - "/mnt/nas/data:/srv/data:rw"
      - "/mnt/nvme/docker/filebrowser/database:/database:rw"
      - "/mnt/nvme/docker/filebrowser/config:/config:rw"
    environment:
      PUID: "1000"
      PGID: "1000"
      UID: "1000"
      GID: "1000"
