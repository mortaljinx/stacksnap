# Reconstructed by StackSnap from running containers.
# This is a best-effort snapshot — review before use.
services:
  librespeed:
    image: lscr.io/linuxserver/librespeed:latest
    restart: unless-stopped
    ports:
      - "8082:80"
      - ":::8082:80"
    volumes:
      - "/mnt/nvme/docker/librespeed:/config:rw"
    environment:
      TZ: "Europe/London"
      PUID: "1000"
      PGID: "1000"
      PS1: "$(whoami)@$(hostname):$(pwd)\$ "
      S6_CMD_WAIT_FOR_SERVICES_MAXTIME: "0"
      S6_VERBOSITY: "1"
      S6_STAGE2_HOOK: "/docker-mods"
      VIRTUAL_ENV: "/lsiopy"
      PHP_INI_SCAN_DIR: ":/config/php"
      LSIO_FIRST_PARTY: "true"
